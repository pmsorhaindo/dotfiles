make distclean && 
    ./configure --prefix ~/***** SOME DIR ***/Work/vim-built --with-features=huge --enable-perlinterp --enable-pythoninterp --enable-rubyinterp --enable-multibyte --enable-fontset && 
        make && 
            make install
